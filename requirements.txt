# Pinned exactly: the .joblib model artifacts in outputs/models/ are only
# valid for the scikit-learn version they were trained with. Loading them
# under a different version silently corrupts RandomForest predict_proba
# (sklearn 1.4 changed tree_.value from class counts to fractions).
scikit-learn==1.5.2
scipy>=1.10.0
pandas>=2.0.0
numpy>=1.24.0,<2.0.0
joblib>=1.3.0
streamlit>=1.32.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
requests>=2.31.0
plotly>=5.18.0
matplotlib>=3.7.0
seaborn>=0.12.0
supabase>=2.3.0
